# 测试任务（QA）

> pytest 单测 + 集成测试 + Locust 压测 + 合规验收 + 海外节点实测 + 跨租户隔离。
> 状态：`[ ]` 未开始 / `[~]` 进行中 / `[✓]` 已完成 / `[-]` 跳过（写明原因）。

---

## 进度总览

- [✓] **T1 后端单测（pytest）**：3/3 核心模块覆盖（hmac / upgrade_engine / signing_service）
- [ ] **T2 集成测试 + 跨租户隔离**：0/4
- [✓] **T3 压测脚本骨架**：1/1（locustfile.py 就位）
- [ ] **T4 性能基线 pin**：0/4（本地未跑出基线数）
- [ ] **T5 海外节点 TTFB 实测**：0/4（依赖 P0 + P5 部署）
- [ ] **T6 合规 / 灰度 / 演练**：0/6
- [ ] **T7 Android 测试**：0/5（依赖 Android 工程开工）

---

## T1 后端单测（pytest）

- [✓] **T1.1** `tests/test_hmac_verifier.py`（15 tests）— 正确签名 / 时间过期 / 签名错误 / app_id 不存在 / 常数时间比较
- [✓] **T1.2** `tests/test_upgrade_engine.py`（13 tests）— 版本范围 / 灰度 / 强升覆盖 / Play 排除 / 过期 / 优先级 / 同 target 多规则
- [✓] **T1.3** `tests/test_signing_service.py`（6 tests）— fan-out 幂等 / check_and_mark_ready 状态机

**待补**（增量）：
- [ ] `tests/test_app_registry.py` — tenant_uuid 不存在 / suspended / 缓存命中 / 缓存失效
- [ ] `tests/test_rate_limit.py` — Redis 与内存两种 backend / TTL 滑动窗口边界
- [ ] `tests/test_video_visibility.py` — region_visibility 默认 false / 黑名单制 / vod_status filter
- [ ] `tests/test_secondary_review.py` — draft↔pending↔approved/rejected 状态机
- [ ] `tests/test_otp_throttle.py` — 三层频控（号 60s / IP 5min/5 次 / 设备 10 次/天）
- [ ] `tests/test_jwt_rotation.py` — refresh 旋转 + 旧 token 黑名单生效

---

## T2 集成测试 + 跨租户隔离

- [ ] **T2.1** **跨租户隔离测试**（红线层级，必须有）：
  - 用 admin A（app_scope=[appA]）登录，尝试访问 appB 的 cp_apps / channels / versions / rules / signing-jobs → 全部 403/404
  - 用 appA 的 hmac_secret 签名调 appB 的 /upgrade/check → 401
  - 同一管理员用 app_scope=[appA, appB] 时能切换看两份数据，互不污染
- [ ] **T2.2** **HMAC 错误注入**：
  - 时间戳过期（now-301s）→ 401
  - 时间戳超前（now+301s）→ 401
  - 签名字段乱序 → 401
  - 错的 secret 签名 → 401
  - 缺 X-CP-Signature header → 401
- [ ] **T2.3** **Walle 签名端到端**：
  - 注册租户 → 上传 APK → finalize → poll signing-jobs 直到 success → 调 /apk/{tenant_uuid}/{channel}/{version_code} → 302 → 下载 APK → SHA256 校验匹配 cp_apk_signing_jobs.output_sha256
- [ ] **T2.4** **VOD reconcile 双向 diff**：
  - 本地有 VOD 没的影片 → reconcile 输出 missing_remote
  - VOD 有本地没的 → reconcile 输出 extra_remote
  - 双方都没的 → 空列表

---

## T3 压测

- [✓] **T3.1** `scripts/locustfile.py`：HmacUser 真实签名 /upgrade/check + 多渠道/国家/版本号 random；本地起 backend 即可跑

---

## T4 性能基线 pin

> 红线 9.4 升级规则引擎性能目标：p50 < 50ms（Redis 命中）/ p95 < 200ms（DB）/ p99 < 500ms。
> 上线前必须有压测数支撑。

- [ ] **T4.1** 本地起 backend + Redis + 1000 条 cp_upgrade_rules 假数据 → Locust 1000 并发打 /upgrade/check → 输出 p50/p95/p99 → 写入 `docs/perf-baseline.md`
- [ ] **T4.2** 模拟缓存击穿：每次 device_id 不同，Redis 命中率 0% → 压观察 DB QPS 上限
- [ ] **T4.3** 模拟批量规则改动：每秒 100 次 invalidate_pattern → 观察 Redis SCAN 性能
- [ ] **T4.4** 母包签名吞吐：单 worker 多渠道 fan-out → 观察 Walle CLI 实际 1-3s 假设是否成立

---

## T5 海外节点 TTFB 实测（依赖 P0 + P5 部署）

- [ ] **T5.1** webpagetest.org SEA（印尼 / 越南 / 菲律宾 / 泰国）4 个节点起播延迟 + 首屏
- [ ] **T5.2** 中东节点（迪拜 / 沙特 / 埃及）实测
- [ ] **T5.3** 拉美节点（巴西 / 墨西哥 / 阿根廷）实测
- [ ] **T5.4** 非洲节点（南非 / 尼日利亚 / 肯尼亚）实测

每个节点输出：TTFB / DNS / TCP / TLS / 首字节 / 首屏 / 卡顿率。**Cloudflare 海外流量套源开关 on/off 各跑一次**对比。

---

## T6 合规 / 灰度 / 演练

- [ ] **T6.1** **RAM 子账号清理 SOP 演练**（P6.9）：建一个测试账号 → 模拟离职 → 走 SOP 同步禁用 → 确认 a_admin_users.app_scope 同步 + ActionTrail 记录
- [ ] **T6.2** **HMAC 密钥泄露应急演练**：
  - 跑 `scripts/cp_rotate_hmac.py`
  - 验证旧 secret 24h 内仍能签名（dual-accept）
  - 24h 后旧 secret 全部失败
- [ ] **T6.3** **强升 / 灰度规则演练**：
  - 创建一条 priority=100 强升规则 + priority=50 50% 灰度规则
  - 多 device_id 调 /upgrade/check 验证 hash_bucket 命中分布近似 50%
  - 强升优先级压灰度（同时命中时返强升）
- [ ] **T6.4** **VOD 控制台 RAM 子账号演练**（依赖 P0）：
  - 用 MovieVodOps RAM 登 VOD 控制台 → 确认只能上传转码 + 不能改 RAM
  - 用 MovieBackendVodReader 调 GetPlayInfo → 通过；调 ModifyMedia → 拒绝
- [ ] **T6.5** **Play 包反编译验证**（红线 #2 兜底）：
  - 拉 release apk-gp-release.apk → unzip + grep SelfUpgradeManager → 必须为空
  - dexdump / jadx 反编译 classes.dex → grep SelfUpgradeManager → 必须为空
- [ ] **T6.6** **隐私 / 删除账号合规验收**：
  - 调 `POST /users/me/delete-request?confirm=true` → 校验 user.status=deleted + 所有 RefreshToken revoked
  - 30 天后异步真删 PII（v1 留 TODO，验收时 verify scheduled job）

---

## T7 Android 测试（依赖 android/ 工程开工）

- [ ] **T7.1** Unit test：HMAC 签名工具类（与 backend 对齐 fixture）
- [ ] **T7.2** Instrumentation test：UpgradeDialog 渲染 + 弹窗策略持久化
- [ ] **T7.3** Espresso：登录流 / 播放流端到端
- [ ] **T7.4** Crash 测试：网络异常 / 401 / 429 / 500 错误友好提示
- [ ] **T7.5** 性能：首页冷启动时间 / 播放起播时间 / 内存峰值

---

## 测试覆盖矩阵（红线对照）

| 红线 | 测试落点 |
|---|---|
| #1 keystore 三地备份 | T6.1 RAM 演练（流程类） |
| #2 Play 包硬关自升级 | T6.5 反编译验证 |
| #3 视频明文防护 | T7.5 + 后端 app_sign 中间件单测 |
| #5 OTP 前置 Turnstile + 国家号段 + 三层频控 | T1 待补 test_otp_throttle |
| #6 VOD 每日 reconcile | T2.4 |
| #7 /upgrade/check HMAC | T1.1 + T2.2 |
| #8 region_visibility 默认 false | T1 待补 test_video_visibility |
| #14 import-linter | CI 自动跑 |
| #15 media_provider 抽象 day 1 | 单测注入 fake provider |

---

## 测试工具与命令

```bash
# 后端单测
cd backend
uv run pytest -v

# 后端单测 + 覆盖率
uv run pytest --cov=app --cov-report=term-missing

# Locust 压测（本地）
cd backend
uv run locust -f scripts/locustfile.py --host http://localhost:8000

# admin-web build（CI 用）
cd admin-web
pnpm build

# import-linter（CI 用）
cd backend
uv run lint-imports

# Gitleaks（CI 用）
gitleaks detect --config .gitleaks.toml
```

---

## QA 缺口清单

> 当前没有专职 QA，单人开发；所有测试任务由开发兼。下列缺口建议交接前补齐。

| 缺口 | 风险 | 优先级 |
|---|---|---|
| 无端到端 UI 自动化（Playwright/Cypress 测 admin-web） | 复杂表单回归靠手测 | 中 |
| 无负载测试 baseline 文档化 | 上线后突发流量摸不清扛不扛得住 | 高 |
| 无安全渗透测试 | OWASP Top 10 没系统过 | 高（上线前必做） |
| 无 Chaos 演练（kill RDS / Redis / ECS 看恢复） | incident-playbook 是写的，没演练过 | 中 |
| 无国际化 UI 测试（9 种语言长文本溢出 / RTL 阿语） | 中东用户体验崩 | 上线前 |
