# 管理后台任务（admin-web）

> Vue3 + Vite + TS + Element Plus + Pinia。
> 目录结构与 backend 模块对齐：`src/modules/{channel-pack,content,user,admin}/{views,api,stores,types}/`。
> 状态：`[ ]` 未开始 / `[~]` 进行中 / `[✓]` 已完成 / `[-]` 跳过（写明原因）。

---

## 进度总览

- [✓] **P1 admin-web 骨架**：5/5（全部完成；从 nextstream 搬迁见 [`../archive/admin-migration.md`](../archive/admin-migration.md)）
- [✓] **P2 App 分发平台页面**：6/6
- [✓] **P3 admin 鉴权 + RBAC 页面**：3/3
- [✓] **P4 content + 二审 + 地区可见性页面**：3/3
- [✓] **P5+ 仪表盘**：1/1
- [ ] **后续可选**：客户端埋点封装 + 多租户切换 UX 优化

---

## P1 admin-web 骨架（部分由 nextstream/admin 搬迁）

- [✓] **1.22** 从 `/Users/yikong/Downloads/nextstream/admin/` 搬迁基础骨架到 `admin-web/`（package.json / vite.config / tsconfig / Layout / Login / icons），删除 nextstream 自带的业务 stores 与 views
- [✓] **1.23** 重组 `admin-web/src/modules/{channel-pack,content,user,admin}/{views,api,stores,types}/` 模块化目录
- [✓] **1.24** 重写 API 客户端：BASE_URL 指 FastAPI（/api/v1）、改 token key 为 `mv_admin_token` / `mv_admin_refresh`、适配 FastAPI `detail` 错误字段
- [✓] **1.25** Placeholder 页面已有，已与 backend 联通（curl + 浏览器登录均通过）
- [✓] **1.26** admin-web build & dev 已跑通（pnpm dev 监听 :5174，已验证登录链路）

---

## P2 App 分发平台 admin-web 页面

- [✓] **2.33** 登录页（之前 P3.13 已完成）
- [✓] **2.34** App 租户管理页（仅超管可见 + 一次性密钥显示对话框 + 重生密钥 + 选为当前）
- [✓] **2.35** 渠道管理页（含 Play 渠道警告标识）
- [✓] **2.36** APK 母包上传页（XHR 进度条 + 多语言 changelog 编辑 + Finalize 按钮）
- [✓] **2.37** 升级规则配置页（含规则预览 + 灰度区间显示百分比 + popup_strategy + 4 个 i18n 文案 tab）
- [✓] **2.38** 签名 job 状态页（按 vc/status 过滤 + 失败手动 retry）

---

## P3 admin 鉴权 + RBAC 页面

- [✓] **3.13** 登录页 / Layout（按 hasPermission 过滤菜单 + 显示当前租户 + token 自动注入）
- [✓] **3.14d** 前端路由 / 组件级 v-if 权限校验（router beforeEach + auth.hasPermission 已落地）
- [✓] **3.14c** admin-web 权限管理页：
  - RolesView（角色列表 + 权限矩阵勾选含模块全选）
  - AdminsView（管理员 CRUD + 角色分配 + app_scope 多租户隔离 + 软删 suspended）
  - 状态机：super_admin 短路 + 内置角色不可删 + 删除前校验是否仍有用户绑定 + 自禁锁防 lockout

---

## P4 content + 地区可见性 + 二审 admin-web 页面

- [✓] **4.14** VideosView 加 Sync All / 对账 / 单行同步按钮（VOD stub 模式提示 + 真模式刷状态）
- [✓] **4.19** VideosView：列表 + 4 tab 编辑（基础/i18n/资源-VOD/上下线-推荐）+ 地区可见性矩阵勾选 + 二审下拉 submit/approve/reject + VOD 状态/二审状态/推荐位标签
- [✓] **4.20** CategoriesView：列表 + 多语言名编辑（9 lang）+ 父子分类 + 软删归档

---

## P5+ 仪表盘

- [✓] **5.18** DashboardView：6 KPI 卡（含 DAU/DAA）+ 4 ECharts line 图 + period 切换（24h/7d/30d）+ trend 切换（7/14/30/90d）

---

## 后续可选 / 增量

- [-] **5.15** App / admin-web 客户端埋点封装 ← 原因：admin-web 部分等业务页面铺开后再加；App 部分由 App 团队接

待做：
| 任务 | 价值 |
|---|---|
| admin-web 业务页面客户端埋点（点击 / 提交 / 错误自动上报） | 完成 P5+.15 闭环 |
| 多租户切换 UX 优化（顶栏明显的 App 切换器 + 数据隔离视觉提示） | 防误操作另一租户 |
| 用户管理页（user 模块）补全：列表 / 搜索 / 详情 / 软删 / 标记封禁 | backend 已有 3 端点等前端接 |
| 操作审计日志页（a_operation_logs）查询 / 筛选 / 详情 | 红线 9 + RAM 子账号关联（external_identity_id） |
| 改密二次校验 UI（敏感操作如重生 hmac_secret 二次输密码） | 红线 16 之外的人因防御 |
| 弹窗策略可视化预览（popup_strategy 6 选 1 + 自定义 hour） | 运营调试体验 |
| 灰度区间可视化（拖拽 0–99 滑块代替手输 device_id_hash_mod_min/max） | 运营调试体验 |
| 升级规则编辑器：拖拽优先级 + 同 target 多规则组（强升 + 灰度组合） | 复杂规则可视化 |
| 暗色主题 + 国际化（运营如果跨国办公） | 长期 |
